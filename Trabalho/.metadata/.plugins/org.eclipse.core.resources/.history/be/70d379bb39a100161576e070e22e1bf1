import java.awt.Color;

import javax.swing.*;

public class Main
{
	public static void main (String[] args)
	{
		JPanel p = new JPanel ();
		p.add (new JButton ("Botão 1"));
		p.setBackground (Color.WHITE);
		p.setSize (400, 300);
		
		Frame f = new Frame ();
		f.getContentPane ().add (p);
		f.setSize (800, 300);
		f.setTitle ("Batalha Naval");
		f.setVisible (true);
	}
}